

export type Product = {
    depth: string | null,
    height: string | null,
    price : string,
    product_id: number,
    quantity: number,
    free_shipping: boolean,
    variant_id: number,
    weight: string,
    width: string | null
  }